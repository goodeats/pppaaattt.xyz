import { Stack, Text } from '@chakra-ui/react';

export default function ContainerIndexPage() {
  return (
    <Stack width="full" paddingX={8} paddingY={5} textAlign="left">
      <Text fontSize="sm">Select a Container or create a new one</Text>
    </Stack>
  );
}
